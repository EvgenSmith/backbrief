---
type: roadmap
schema_version: 1
---

# Backbrief roadmap — deferred setup steps

> Machine-regenerated by `plugin/scripts/state.js` on every step write — do not
> edit by hand. Each entry names what was deferred, what it costs, and the command
> that resumes it. `/backbrief status` shows the same summary.
> (This is the born-empty skeleton copy; the first `state.js set steps.*` write —
> or `state.js roadmap` — replaces it with the live version.)

## Deferred steps

None. Nothing is deferred — every visited step is either done or still ahead.

## Watching for connectors

None observed yet. Tools named during setup that the kit does not ship land in
`.backbrief/waitlist.yaml` (`state.js waitlist-observe`) and are mirrored here —
demand counts toward the public connector roadmap.

## Next suggested rung

**A0 — vault + survey** → `/backbrief start`
