# general/ — cross-team & unresolved calls

The mixed route: calls that span teams, all-hands, external-NDA calls, and
calls whose team could not be resolved. A real team folder, not a junk drawer —
digests here get full treatment. Calls land in `transcripts/`.
