# product/ — product team calls

Golden-path default team folder (tenant.yaml `vault.teams[]` — rename or
replace to match your org). Calls land in `transcripts/`; add subfolders
(`decisions/`, `docs/`, `research/`) only the day the first file needs them.
